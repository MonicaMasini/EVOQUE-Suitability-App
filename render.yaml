services:
  - type: web
    name: evoque-suitability
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "streamlit run web_app.py --server.port $PORT --server.address 0.0.0.0"
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.10

